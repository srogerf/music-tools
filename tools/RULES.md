# Rules for tools in tools/

These rules apply to all code generated under `tools/`.

- Language: Go (Golang).
- Structure: each immediate subdirectory under `tools/` is a separate tool.
- Interface: each tool provides a simple command-line interface.
- Scales: include harmonic and melodic minor variants when relevant.
