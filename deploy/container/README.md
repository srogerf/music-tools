# Container

This directory is for containerization files and related build assets.

Create one subdirectory per container type or packaging approach.

Examples:

- `docker/`
- `podman/`

Each type directory can contain its own build files, helper scripts, and image-specific documentation.
