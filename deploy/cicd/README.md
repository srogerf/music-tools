# CI/CD

This directory is for deployment automation and configuration management files, such as Chef or Ansible.

Create one subdirectory per tool or deployment type.

Examples:

- `ansible/`
- `chef/`

Each tool directory can contain its own playbooks, cookbooks, roles, pipelines, templates, and supporting documentation.
