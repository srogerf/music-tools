# Infrastructure

This directory is for cloud infrastructure creation and provisioning, such as Terraform.

Create one subdirectory per infrastructure provider.

Examples:

- `aws/`
- `gcp/`
- `azure/`

Each provider directory can contain its own modules, variables, environments, and supporting documentation.
