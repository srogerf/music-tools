{
  "goal": "Keep changes small, safe, and well-scoped.",
  "scope": {
    "in": ["repo-only changes"],
    "out": ["tooling changes", "CI changes", "unrelated refactors"]
  },
  "constraints": [
    "no new dependencies without approval",
    "preserve existing behavior unless stated otherwise",
    "keep diffs minimal and easy to review"
  ],
  "process": [
    "ask clarifying questions if requirements are missing",
    "state assumptions explicitly if proceeding",
    "summarize changes and list files touched"
  ],
  "tests": {
    "run_when": ["explicitly requested", "high-risk change"],
    "note_if_skipped": true
  }
}
